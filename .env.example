# Copia este archivo como .env y completa tus claves (nunca subas .env al repo)
ANTHROPIC_API_KEY=tu_api_key_de_anthropic

# Embeddings: "local" (default, sin API key) o "voyage" (requiere VOYAGE_API_KEY)
# EMBEDDINGS_PROVIDER=local
# VOYAGE_API_KEY=tu_api_key_de_voyage_ai
